module Card{
    domain     Card;
    subdomain   Atm;

    provided interface ICard{
            void readCard(in int CardNo, in string Password);
            void ejectCard();
            void consortium();
    } ;      
};
